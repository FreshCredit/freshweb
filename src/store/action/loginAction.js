export const setInputType = (state) => {
  return {
    type: 'SET_INPUT_TYPE',
    payload: state,
  }
}

export const setLoginInfo = (state) => {
  return {
    type: 'SET_LOGIN_INFO',
    payload: state,
  }
}